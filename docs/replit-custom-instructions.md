# Proposed Workspace Custom Instructions

Install this block manually through **Workspace Settings → Customization**. This project file cannot change Workspace Settings. Installation remains pending until the Product Owner completes that step.

```text
- Never expose or commit secrets.
- Never silently change approved shared contracts.
- Never bypass tenant/company/store scoping.
- Never self-certify independent review.
- Significant work requires separate Reviewer and QA passes.
- If task risk is unclear, treat it as Significant.
- Preserve working behavior unless replacement is explicitly approved.
- Document material deviations from the approved task.
```