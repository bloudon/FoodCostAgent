
administrator@vps-BLOUD-Soft:~/apps/CostPro/fnbcostpro$ set -a && . ./.env && set +a && psql "$DATABASE_URL" -X -v ON_ERROR_STOP=1 -c "SELECT failure_reason, count(*) FROM inventory_item_remediation_audit WHERE manifest_id = 'bay-hill-batch1-2026-08-15' AND result = 'stopped' GROUP BY failure_reason ORDER BY count(*) DESC;" -c "SELECT pid, state, wait_event_type, wait_event, now() - query_start AS query_age, left(query, 240) AS query FROM pg_stat_activity WHERE datname = current_database() AND pid <> pg_backend_pid() ORDER BY query_start NULLS LAST;"
                                                                                                                                                                                                  failure_reason                                                                                                                                                                                                  | count
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+-------
 OUT_OF_SCOPE_REFERENCE: OUT_OF_SCOPE_REFERENCE: this group's items are referenced outside the approved scope (store ee9e1530-50db-45f4-ae61-2c45e86827f0, ORDERLY property 24472): external mappings for another source system or property (1). Approval covers one property’s data, so merging would rewrite records it does not authorize — stopping this group without mutation.                              |    12
 OUT_OF_SCOPE_REFERENCE: OUT_OF_SCOPE_REFERENCE: this group's items are referenced outside the approved scope (store ee9e1530-50db-45f4-ae61-2c45e86827f0, ORDERLY property 24472): external mappings for another source system or property (2). Approval covers one property’s data, so merging would rewrite records it does not authorize — stopping this group without mutation.     